To run AI trials simply execute either `python heuristic_checker.py` or
`python compare_heuristics.py`. You can change which heuritics are checked and
the problems that they're tested on inside those files.
